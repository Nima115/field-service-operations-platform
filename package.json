{
  "name": "serviceflow",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack SaaS business management platform for service-based companies.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @serviceflow/api & npm run dev --workspace @serviceflow/web",
    "build": "npm run build --workspace @serviceflow/api && npm run build --workspace @serviceflow/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate --workspace @serviceflow/api",
    "prisma:migrate": "npm run prisma:migrate --workspace @serviceflow/api",
    "seed": "npm run seed --workspace @serviceflow/api"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
